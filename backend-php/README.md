## Optional PHP + MySQL backend (for non-Netlify PHP hosts)

This folder is provided only if you deploy on hosting that supports PHP runtime.
Netlify static hosting does not execute PHP directly.

Use `schema.sql` to initialize DB and `config.php` as DB connection starter.
